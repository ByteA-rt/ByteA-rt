def sum_array(arr):
    #your code here
    if not arr or len(arr) <= 1:
        print(0)
        return (0)
    else:
        excluding = (max(arr) + min(arr))
        s = sum(arr)
        total = s - excluding
        print (total)
        return (total)
