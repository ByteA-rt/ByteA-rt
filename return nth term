def series_sum(n):
    # Happy Coding ^_^

    count = 1
    array = []
    
    while count <= n:
        calculation = (1 / ((3 * count) - 2 ))
        array.append(calculation)
        count += 1

    x = (sum(array))

   return("%.2f" % x)
